{% extends "freeform-demo/_layout" %}
{% set pageTitle = "Extras" %}
{% set page = "extras" %}

{# Replace 'multipleForm' with your form handle. #}
{% set form = craft.freeform.form("multipleForm") %}

{% block content %}

<div class="container">

    <h2>Simple multi-page form manually coded inside template</h2>
    <p>The example showing the iterating through pages</p>
    <hr />

    {% if form %}

        <h3> {{ form.name }} </h3>
        <br />

        {# Create first freeform form and replace "myFirstForm" with your form handle #}
        <div>
            {{ craft.freeform.form('myFirstForm', {
                fieldIdPrefix: 'my-first-form-',
            }).render() }}
        </div>

        <div>
            <p>Description here</p>

            {# Create second freeform form and replace "mySecondForm" with your form handle #}
            {{ craft.freeform.form('mySecondForm', {
                fieldIdPrefix: 'my-second-form-',
            }).render() }}
        </div>

    {% else %}
    {# Show instructions to developer if they wish to see this page work correctly #}

        <div class="alert alert-warning" role="alert">
            <h4 class="alert-heading">You can't see this page correctly until...</h4>
            <p class="mb-0">
                In order to view this page live, you'll need to make some adjustments to this template or on your test from:
                <ol class="mb-0">
                    <li>Rename your test form handle to <code>multipleForms</code> ( or adjust the handle name this template is looking for).</li>
                    <li>
                        Make sure your t est form includes the following fields (at minimum):
                        <ul>
                            <li>Company Name text field with handle of <code>companyName</code>.</li>
                            <li>Email address field with handle of  <code>email</code>.</li>
                            <li>Address text field with handle of <code>address</code>.</li>
                        </ul>
                    </li>
                    <li>The person viewing this form is logged into their Craft account to see this working.</li>
                    <li>The person viewing this form have a first and last name set in their User account.</li>
                </ol>
            </p>
        </div>
    
    {% endif %}

</div>

{% endblock %}
