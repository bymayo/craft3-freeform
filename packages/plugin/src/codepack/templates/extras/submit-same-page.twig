{% extends "freeform-demo/_layout" %}

{% set pageTitle = "Extras" %}
{% set page = "extras" %}

{# Replace "submitSamePage" with your form handle. #}
{% set form = craft.freeform.form("submitSamePage") %}

{% block content %}
    
<div class="container">

    <h2>Returning Submit to Same Page</h2>
    <p>The exapmle adding a query in the success URL instead of an additional segment.</p>
    <hr />

    {% if form %}

        <h3>{{ form.name }}</h3>
        <br />
    
        {% if craft.app.request.getQueryParam('success') == 1 %}
            <div class="callout success">Your message has been sent.</div>
        {% endif %}

        {# Replace"submitSamePage" with your form handle. #}
        {# Replace "freeform-demo/extras/submit-same-page/?success=1" which will be "success" URL page. #}
        {{ craft.freeform.form("submitSamePage", {
            returnUrl: "freeform-demo/extras/submit-same-page/?success=1",
            submitClass: "button",
            }
        ).render() }}

    {% else %}
    {# Show instructions to developer if they wish to see this page work correctly #}

        <div class="alert alert-warning" role="alert">
            <h4 class="alert-heading">You can't see this page correctly until...</h4>
            <p class="mb-0">
                In order to view this page live, you'll need to make some adjustments to this template or your test form:
                <ol class="mb-0">
                    <li>Rename your test form handle to <code>submitSamePage</code> (or adjust the handle name this template is looking for).</li>
                    <li>
                        Make sure your test form includes the following fields (at minimum):
                        <ul>
                            <li>User Name text field with handle of <code>userName</code>.</li>
                            <li>Last Name text field with handle of <code>lastName</code>.</li>
                        </ul>
                    </li>
                    <li>The person viewing this form is logged into their Craft account to see this working.</li>
                    <li>The person viewing this form have a first and last name set in their User account.</li>
                </ol>
            </p>
        </div>
    
    {% endif %}

</div>

{% endblock %}